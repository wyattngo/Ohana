{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "backend",
      "runtimeExecutable": "bash",
      "runtimeArgs": [
        "-c",
        "set -a; source .env; set +a; export OHANA_ENV=dev DATABASE_URL=\"postgresql+psycopg://ohana:${POSTGRES_PW}@localhost:${OHANA_PG_PORT:-5432}/ohana\"; exec uvicorn app.main:app --port 8001 --reload"
      ],
      "port": 8001,
      "autoPort": false
    },
    {
      "name": "web",
      "runtimeExecutable": "web/node_modules/.bin/vite",
      "runtimeArgs": ["web", "--port", "5173", "--strictPort"],
      "port": 5173
    }
  ]
}
