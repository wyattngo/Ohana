# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist untracked kể từ 2026-08 — build artifact tái tạo bằng `pnpm build` (dev) hoặc
# web/Dockerfile stage 1 (prod). App.main dev đã guard `if _WEB_DIST.is_dir()` nên clone
# không có dist vẫn boot OK. Root .gitignore có Python-packaging `dist/` pattern nên phải
# khai lại ở đây (web/dist là JS build), tương tự cho src/lib (Python packaging pattern).
dist
!src/lib/
dist-ssr
web.zip

# Playwright artifacts
test-results/
playwright-report/
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
